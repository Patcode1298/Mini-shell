# Mini Shell (C#)

A simple C# console-based mini shell application.

## Commands
- `/add` – Add a new item
- `/delete` – Delete an item
- `/read` – List all items
- `/settings` – Open settings menu
- `/clear` – Clear console
- `/exit` – Exit the program
